Twoim zadaniem jest wycofanie wszystkich tymczasowych zmian wprowadzonych w poprzednim kroku (przygotowanie ekranów do screenshotów). Żadna z tych zmian nie była commitowana.

Cofnij wszystkie zmiany w working tree (zmodyfikowane i nowo utworzone pliki), zweryfikuj że projekt jest czysty (`git status`, `flutter analyze`).

### ⚙️ AKCJA KOŃCOWA
Daj znać użytkownikowi, że tymczasowy kod screenshotów został usunięty i projekt jest czysty. Poleć mu napisanie `next`.

Gdy napisze `next`, przejdź do `docs/commands/08_revenuecat.md`.
